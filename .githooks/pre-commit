#!/usr/bin/env bash
set -e

branch=$(git symbolic-ref --short HEAD 2>/dev/null || echo "")

if [ "$branch" = "main" ]; then
  echo "🚫 Commit direct sur main interdit — crée une branche (voir .claude/git-conventions.md)." >&2
  exit 1
fi
