#!/usr/bin/env bash
set -e

while read -r local_ref local_sha remote_ref remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "🚫 Push direct sur main interdit — passe par une Pull Request." >&2
    exit 1
  fi
done
