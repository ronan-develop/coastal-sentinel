services:
  database:
    image: mariadb:11.4
    environment:
      MARIADB_DATABASE: coastal_sentinel
      MARIADB_USER: app
      MARIADB_PASSWORD: app
      MARIADB_ROOT_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - database_data:/var/lib/mysql

volumes:
  database_data:
